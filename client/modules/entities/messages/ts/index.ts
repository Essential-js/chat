import { socketConexion } from '@essential-js/client/entities/socket-conexion';

class Messages {
	#items: Array<string>;

	sendMessage(message: string) {}

	receiveMessage(messages: Array<string>) {}
}
